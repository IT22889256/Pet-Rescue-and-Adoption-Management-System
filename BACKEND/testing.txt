// app.delete('/employees/:id', async (req, res) => {
//     const employeeId = req.params.id;
  
//     try {
//       // Find the employee document by ID
//       const employee = await Employee.findOne({ eid: employeeId });
//       if (!employee) {
//         return res.status(404).send('Employee not found');
//       }
  
//       // Create a new document in employee2 collection
//       const employee2 = new Employee2(employee.toObject()); // Copy the employee data
//       await employee2.save();
  
//       // Delete the original employee document
//       await employee.remove();
  
//       res.send('Employee data transferred and deleted successfully');
//     } catch (error) {
//       console.error('Error deleting employee:', error);
//       res.status(500).send('Internal Server Error');
//     }
//   });



//   const deleteEmployee = async (req, res) => {
//     try {
//       const { id } = req.params;
  
//       const employee = await Employee.findByIdAndDelete(id);
  
//       if (!employee) {
//         return res.status(404).json({ message: "Employee not found" });
//       }
  
//       res.status(200).json({ message: "Employee deleted successfully" });
//     } catch (error) {
//       res.status(500).json({ message: error.message });
//     }
//   };
  

// attendanceDate: {
//     type: Date,
//     required: [true, "Please enter the Date"],
//   },


// const Employee = require("./models/employee.model.js"); 
// const DeletedEmployee = require("./models/deleted_employee.model.js");